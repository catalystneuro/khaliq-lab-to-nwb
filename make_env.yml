name: khaliq_lab
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.11
  - pip
  - jupyterlab
  - ipykernel
  - pip:
      - --editable . # This calls the setup and therefore requirements minimal
